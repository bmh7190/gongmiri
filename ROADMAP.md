# Gongmiri Roadmap

공미리는 Shapefile ZIP을 브라우저 안에서 빠르게 검사해 데이터의 공간 분포와 속성 품질을 판단하도록 돕는 Chrome 확장 프로그램이다.

제품이 지향하는 핵심 흐름은 다음과 같다.

> ZIP 다운로드 감지 → 사용자의 한 번의 클릭 → 공미리 전체 화면에서 검사·탐색

Chrome의 로컬 파일 접근 제약 때문에 “다운로드 즉시 무조건 자동 분석”은 목표로 삼지 않는다. 권한과 개인정보를 투명하게 설명하고, 자동 감지를 원하지 않는 사용자는 기존 파일 선택 흐름을 계속 사용할 수 있게 한다.

## 원칙

- 데이터는 기본적으로 브라우저 안에서만 처리하고 외부 서버로 전송하지 않는다.
- 파싱 정확성과 오류 복구를 시각적 완성도보다 우선한다.
- 한국어와 영어를 동일한 1급 언어로 지원한다.
- UI 계층은 React로 통일하고 Vue와 React를 함께 배포하지 않는다.
- 전체 화면 뷰어를 주 사용 환경으로 삼고 작은 팝업에 분석 UI를 넣지 않는다.
- 대용량 데이터에서는 브라우저가 멈추지 않는 것을 전체 데이터 표시보다 우선한다.
- 각 단계는 아래 완료 조건을 충족해야 종료한다.

## Phase 0 — 개발·배포 기준선

목표: 누구나 같은 소스에서 설치 가능한 확장 빌드를 재현한다.

- [x] React 마이그레이션 결정을 [ADR 001](docs/architecture/001-ui-framework.md)에 기록한다.
- [x] `package.json`과 확장 매니페스트 버전을 일치시킨다.
- [x] `dist/`와 React 비교 산출물을 `.gitignore`에 두고 빌드 산출물로만 관리한다.
- [x] 전체 화면 뷰어와 확장 아이콘 동작을 일치시킨다.
- [x] 사용하지 않는 Vite 초기 샘플 컴포넌트와 전역 CSS를 제거한다.
- [x] `npm ci`, 테스트, 타입 검사, 프로덕션 빌드를 CI에서 실행한다.
- [x] Chrome에서 압축 해제 확장으로 로드하는 [릴리스 체크리스트](docs/release-checklist.md)를 문서화한다.
- [x] 500kB를 넘는 지도 런타임은 지연 로딩한 별도 청크로 분리한다.

완료 조건:

- 깨끗한 체크아웃에서 `npm ci && npm run build`가 성공한다.
- 생성된 `dist/`를 Chrome에 로드했을 때 매니페스트 및 런타임 오류가 없다.
- 표시되는 확장 이름과 버전이 소스의 값과 일치한다.

## Phase 1 — React 마이그레이션

목표: 기존 기능을 보존하면서 UI 계층을 React로 교체하고 책임 경계를 다시 설계한다.

마이그레이션은 기존 화면의 픽셀 단위 복사가 아니다. 데이터 처리 결과와 핵심 사용자 흐름은 동등하게 유지하되, 각 기능을 이식할 때 아래 개선을 함께 적용한다.

- 거대한 컴포넌트를 feature 컴포넌트와 hook으로 분리한다.
- 로딩·오류·빈 상태를 명시적인 상태 모델로 통합한다.
- 전체 탭과 좁은 화면에서 사용할 수 있는 반응형 레이아웃으로 바꾼다.
- 키보드 포커스, 레이블, `aria-live` 등 기본 접근성을 적용한다.
- 하드코딩된 문구를 추가하지 않고 모든 새 UI를 한국어·영어로 작성한다.
- 지도·테이블 선택처럼 공유되는 상태의 소유자를 명확히 한다.

검색, 내보내기, 다운로드 감지와 같은 신규 제품 기능은 React 기능 동등성 이후 단계에서 진행한다. 이식 중에는 기존 기능 복구와 직접 관련된 개선만 포함한다.

- [x] 대표 샘플 ZIP과 프로덕션 빌드를 [기능 비교 기준](docs/migration/react-baseline.md)으로 기록한다.
- [x] 파서, SRID, 통계, 시각화 계산을 프레임워크 독립 모듈로 이동한다.
- [x] React, React DOM, Vite React 플러그인과 독립 타입 검사 기반을 추가한다.
- [x] 업로드 → ZIP 검사 → Worker 파싱 → 결과 요약 수직 기능을 먼저 이식한다.
- [x] MapLibre 수명주기와 지도 선택 상태를 React hook으로 이식한다.
- [x] 속성 테이블과 가상 스크롤을 이식한다.
- [x] 시각화 설정을 이식한다.
- [x] Quick/Full 모드를 이식한다.
- [x] 기능 동등성 검증 후 Vue entrypoint, SFC와 Vue 의존성을 제거한다.
- [x] 프로덕션 진입점을 React로 전환하고 번들에 React 런타임만 포함되는지 확인한다.

완료 조건:

- 기존에 지원하던 대표 ZIP의 결과와 사용자 흐름이 React 버전에서 일치한다.
- `App.tsx`와 지도 컴포넌트에 파싱·통계·타일 계산이 직접 포함되지 않는다.
- `npm run build` 결과에 Vue 런타임과 `.vue` 진입점이 포함되지 않는다.
- 생성된 `dist/`가 Chrome에서 오류 없이 열린다.
- 모바일 폭과 키보드 탐색에서 기존 Vue 화면보다 기능 접근성이 나빠지지 않는다.

## Phase 2 — 다국어 기반과 UI 토대

목표: UI 개편 전에 모든 사용자 문구를 한국어·영어로 안정적으로 제공한다.

### 다국어

- [x] React 화면용 `ko`/`en` 메시지 카탈로그와 번역 함수를 추가한다.
- [x] 브라우저 언어를 최초 기본값으로 사용한다.
- [x] 언어 선택값을 `chrome.storage`에 저장한다.
- [x] 화면의 `lang`과 `title`을 선택 언어에 맞춰 갱신한다.
- [x] Chrome `_locales` 메시지로 확장 이름과 설명을 번역한다.
- [x] React 컴포넌트에 직접 작성된 한국어 문구를 번역 키로 이동한다.
- [x] Worker는 번역 문장 대신 진행 단계·오류 코드를 반환한다.
- [x] 숫자, 파일 크기, 피처 수를 현재 locale로 포맷한다.

### UI 토대

- [x] 색상·간격·타이포그래피·상태 표현을 CSS 토큰으로 정의한다.
- [x] 버튼, 필드, 배지와 알림의 활성·포커스·비활성·오류 상태를 정리한다.
- [x] 320px 이상의 좁은 화면과 전체 탭 레이아웃을 함께 지원한다.
- [x] 키보드 포커스와 `aria-live` 진행 안내를 추가하고 새 모달은 focus trap을 필수로 한다.
- [x] 한국어·영어 및 200% 확대 유효 폭에서 레이아웃을 검증한다.

완료 조건:

- 사용자에게 노출되는 핵심 흐름에 하드코딩된 한국어가 없다.
- 한국어와 영어를 새로고침 없이 전환할 수 있고 선택이 유지된다.
- Chrome 확장 관리 화면의 이름과 설명도 브라우저 언어에 맞게 표시된다.
- 키보드만으로 파일 선택, 언어 전환, 모달 선택이 가능하다.

## Phase 3 — 뷰어 정보 구조 개편

목표: 사용자가 파일의 사용 가능성을 빠르게 판단하도록 핵심 정보를 재배치한다.

- [x] 상단에 파일명, 파일 크기, 피처 수, SRID, 인코딩, Quick/Full 상태를 고정한다.
- [x] 지도, 속성 테이블, 데이터 품질, 파일 정보 영역을 명확히 분리한다.
- [x] 좁은 화면에서는 주요 영역을 탭으로 전환한다.
- [x] 업로드 첫 화면의 설명을 줄이고 기본 행동을 명확하게 만든다.
- [x] 누락된 SHP 구성 파일과 해결 방법을 레이어별로 구체적으로 표시한다.
- [x] 컬럼을 채움률, NULL, 고유값, 타입 이상 기준으로 탐색하게 한다.
- [x] 빈 값이 많거나 단일 값·혼합 타입 컬럼에 품질 경고를 제공한다.

완료 조건:

- 파일을 연 뒤 별도 스크롤 없이 파일명, 피처 수, 좌표계, 인코딩을 확인할 수 있다.
- 지도와 테이블에서 선택된 피처가 항상 동일하다.
- 오류 메시지가 문제 파일과 사용자가 취할 행동을 함께 설명한다.

## Phase 4 — 파싱 신뢰성과 오류 복구

목표: 지원 형식에서 잘못된 결과를 조용히 보여주는 일을 방지한다.

- [x] UTF-8, CP949, EUC-KR DBF 테스트 픽스처를 추가한다.
- [x] EPSG 4326, 3857, 5179, 5186 좌표 변환을 검증한다.
- [x] Point, LineString, Polygon, MultiPolygon을 검증한다.
- [x] 다중 레이어, 한글 파일명, 중첩 폴더 ZIP을 검증한다.
- [x] 필수 파일 누락, 손상 ZIP, 좌표계 판별 실패를 구분한다.
- [x] 파싱 취소 시 Worker를 교체하고 같은 설정으로 재시도를 지원한다.
- [x] 베이스맵 실패 시에도 속성 분석 결과를 유지한다.

완료 조건:

- 대표 픽스처의 피처 수, 속성, 좌표가 자동 테스트로 검증된다.
- 예상 가능한 실패 유형마다 고유 오류 코드와 복구 안내가 있다.
- 새 파일을 열거나 취소했을 때 이전 Worker 결과가 화면을 덮어쓰지 않는다.

## Phase 5 — 다운로드 감지 PoC

목표: 과도한 권한 없이 다운로드 이후의 반복 동작을 줄일 수 있는지 검증한다.

- [x] 선택형 `downloads` 권한으로 ZIP 완료 이벤트를 감지한다.
- [x] ZIP 파일명 또는 URL 후보만 확장 아이콘 배지로 제안한다.
- [x] 자동 감지 기능을 사용자가 켜고 끌 수 있게 한다.
- [x] 배지가 표시된 확장 아이콘을 누르면 전체 화면 뷰어를 연다.
- [x] 로컬 파일 접근, 원본 URL 재요청, 파일 재선택 방식을 [PoC 문서](docs/download-detection-poc.md)에서 비교한다.
- [x] 로그인, POST, 일회성 URL, 이름이 잘못된 ZIP의 예상 실패 동작과 Chrome 검증 항목을 기록한다.
- [x] 필요한 권한과 로컬 처리 방식을 뷰어에서 설명한다.

완료 조건:

- Windows Chrome의 대표 다운로드 유형에서 성공률과 실패 원인이 문서화된다.
- 실패하더라도 기존 드래그 앤 드롭·파일 선택 흐름으로 복구할 수 있다.
- 사용자의 명시적 설정 없이 광범위한 파일 접근을 요구하지 않는다.

PoC 결과가 안정적이지 않으면 제품 문구를 “다운로드 완료 알림 후 빠른 파일 선택”으로 제한한다.

## Phase 6 — 탐색과 내보내기

목표: 미리보기에서 발견한 결과를 실제 분석 흐름으로 가져갈 수 있게 한다.

- [x] 컬럼명 및 값 검색을 추가한다.
- [x] NULL 여부, 값, 숫자 범위로 정렬·필터링한다.
- [x] 테이블 필터 결과를 지도와 내보내기 범위에 연결한다.
- [x] 전체·선택·필터 결과를 GeoJSON으로 저장한다.
- [x] 속성을 UTF-8 CSV로 저장한다.
- [x] Quick 결과 내보내기에는 샘플 데이터임을 명시한다.
- [x] 필드 선택과 원본 좌표계/WGS84 선택을 제공한다.

완료 조건:

- 필터 결과 개수와 지도·테이블·내보내기 결과가 일치한다.
- 한글 속성을 포함한 CSV가 Excel과 일반 텍스트 도구에서 정상적으로 열린다.
- 대용량 내보내기가 실패할 때 메모리 한계와 대안을 안내한다.

## Phase 7 — 대용량 최적화와 품질 마감

목표: 실제 공공 공간데이터 규모에서 UI 중단과 메모리 급증을 줄인다.

- [x] Quick 샘플링과 통계 계산을 Worker 내부로 이동한다.
- [x] 메인 스레드로 전달하는 데이터 양을 줄인다.
- [x] 압축 해제, DBF, SHP, 재투영, 통계, 렌더 단계를 각각 표시한다.
- [x] 피처 수뿐 아니라 좌표 수와 파일 크기로 처리 모드를 결정한다.
- [x] 지도 번들을 지연 로딩해 초기 번들 크기를 줄인다.
- [x] 접근성, 다크 모드, Chrome Web Store 설명과 개인정보 문서를 마무리한다.

성능 목표:

- 50MB·5만 피처 대표 파일의 Full 파싱을 기준 장비에서 5초 안에 완료한다.
- 100MB·10만 피처 대표 파일의 Quick 첫 결과를 4초 안에 표시한다.
- 파싱 중 파일 선택, 취소, 화면 이동에 1초 이상의 UI 정지가 발생하지 않는다.

Node 등가 로직 기준 측정값은 [성능 기준선](docs/performance-baseline.md)에 기록하며, 10만 Point Quick 선행 샘플은 3.69초를 기록했다.

## 남은 출시 검증

1. `dist/`를 Windows Chrome에 직접 로드해 서비스 워커, 선택 권한, 취소·재시도를 검증한다.
2. 실제 Chrome 200% 확대와 운영체제 다크 모드에서 한국어·영어 레이아웃을 확인한다.
3. 100MB·10만 피처 대표 파일의 Quick 첫 결과를 실제 Chrome에서 측정한다. 5만 피처 기준은 [성능 기준선](docs/performance-baseline.md)에 기록했다.
4. 공개 개인정보 처리방침 URL과 지원 이메일을 스토어 등록 문안에 반영한다.
